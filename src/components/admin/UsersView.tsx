import React, { useState } from 'react';
import { useRestaurant } from '../../context/RestaurantContext';
import { UserAccount, UserRole } from '../../types';
import { ShieldCheck, Plus } from 'lucide-react';
const roles: UserRole[] = ['administrador','gerente','caixa','garcom'];
export const UsersView: React.FC = () => { const {users,saveUser}=useRestaurant(); const [editing,setEditing]=useState<UserAccount|null>(null); const empty:UserAccount={id:'',nome:'',usuario:'',cargo:'',perfil:'garcom',ativo:true,permissoes:['pdv','pedidos','mesas']}; const save=()=>{if(!editing?.nome||!editing.usuario)return;saveUser({...editing,id:editing.id||`usr-${Date.now()}`});setEditing(null)}; return <div className="max-w-7xl mx-auto px-4 sm:px-6 py-6 space-y-5"><div className="flex justify-between"><div><h2 className="text-xl font-bold font-serif flex gap-2 items-center"><ShieldCheck className="w-6 h-6 text-blue-600"/>Usuários & Permissões</h2><p className="text-xs text-stone-500">Perfis e permissões operacionais do PDV.</p></div><button onClick={()=>setEditing(empty)} className="px-4 py-2 bg-blue-600 text-white rounded-xl text-xs font-bold"><Plus className="w-4 h-4 inline mr-1"/>Novo usuário</button></div><div className="bg-white border rounded-2xl overflow-hidden"><table className="w-full text-xs"><thead className="bg-stone-50"><tr><th className="p-3 text-left">Nome</th><th className="p-3 text-left">Usuário</th><th className="p-3 text-left">Perfil</th><th className="p-3">Ativo</th><th className="p-3"></th></tr></thead><tbody>{users.map(u=><tr key={u.id} className="border-t"><td className="p-3 font-bold">{u.nome}</td><td className="p-3">{u.usuario}</td><td className="p-3 uppercase">{u.perfil}</td><td className="p-3 text-center">{u.ativo?'Sim':'Não'}</td><td className="p-3 text-right"><button onClick={()=>setEditing(u)} className="text-blue-700 font-bold">Editar</button></td></tr>)}</tbody></table></div>{editing&&<div className="fixed inset-0 z-50 bg-black/50 flex items-center justify-center p-4"><div className="bg-white rounded-2xl p-6 w-full max-w-md space-y-3"><h3 className="font-bold">Usuário</h3><input value={editing.nome} onChange={e=>setEditing({...editing,nome:e.target.value})} placeholder="Nome" className="w-full p-2.5 border rounded-xl"/><input value={editing.usuario} onChange={e=>setEditing({...editing,usuario:e.target.value})} placeholder="Usuário" className="w-full p-2.5 border rounded-xl"/><input value={editing.cargo} onChange={e=>setEditing({...editing,cargo:e.target.value})} placeholder="Cargo" className="w-full p-2.5 border rounded-xl"/><select value={editing.perfil} onChange={e=>setEditing({...editing,perfil:e.target.value as UserRole})} className="w-full p-2.5 border rounded-xl">{roles.map(r=><option key={r}>{r}</option>)}</select><label className="flex gap-2 text-sm"><input type="checkbox" checked={editing.ativo} onChange={e=>setEditing({...editing,ativo:e.target.checked})}/> Ativo</label><div className="flex justify-end gap-2"><button onClick={()=>setEditing(null)} className="px-4 py-2 bg-stone-100 rounded-xl text-xs font-bold">Cancelar</button><button onClick={save} className="px-4 py-2 bg-blue-600 text-white rounded-xl text-xs font-bold">Salvar</button></div></div></div>}</div> };
